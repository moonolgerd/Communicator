<Window x:Class="Communicator.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Communicator.Client"
        mc:Ignorable="d"
        Title="Client" Height="484" Width="354">
    <Window.DataContext>
        <local:MainWindowViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Label Content="{Binding Status}" Grid.Row="0"/>
        <StackPanel Grid.Row="1">
            <TextBox Text="{Binding Text}" />
            <Button Command="{Binding SendBack}">Send</Button>
        </StackPanel>
    </Grid>
</Window>
